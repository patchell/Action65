MODULE

INT Avar,x,y,z
CARD Cvar
CHAR Stuff,Yep
TYPE  Junk = [
	CHAR q,e,d
	INT POINTER pX
]
INTERRUPT Hey()
	TYPE Crud = [
		INT CrudOne
		CHAR CrudTwo
	]
	INT POINTER ARRAY POINTER cc
	CHAR la,lb
	CARD lc,ld,le
	BYTE lf

	ASM {
		SECTION "PageZero" [
			START = $0000
			SIZE = $100
			MODE = READ_WRITE
			ZEROPAGE = TRUE
		]
		:First	DB	0
		:Second	DB	1

		LDA #$4A
		LDA #>$ABCD
		LDA #<$ABCD
		LDA First
	}
	IF la = lb THEN
		la ==+ lb - le
	ELSEIF ld < lf THEN
		la = lb * le
	ELSEIF ld > lf THEN
		la = lb / le
	ELSE
		EXIT
	FI
	FOR lf = 0 TO 14 STEP 2
	DO
		la = lf RSH ld
	OD
	WHILE lc < ld
	DO
		la = lf LSH ld
	OD
	DO
		la = lb * lc - lf
		EXIT
	OD
	BREAK
	RTI
	EXIT
	RETURN
	RETURN la - lb

	Avar = x + y
	Avar ==* x + y
	Avar ==& x + y
	Avar ==% x + y
	Avar ==LSH x + y
	Avar ==XOR x + y
	Avar ==MOD x + y
	RETURN

MODULE
CHAR xyx, yxy
CARD one,two, three


